# Recommended models (assistant spec §8, §10), shown by
# `openvibes-admin assistant check`. Updated with each release after the
# quality gate has run on the model: `sha256` is the tested GGUF file and
# `tested` the date. An empty `tested` means the recommendation is not yet
# measured on OpenVIBES; run `openvibes-admin assistant eval` before relying
# on it. All are Apache-2.0 and support tool calling; use non-thinking mode.
# File names follow the usual GGUF naming; check them when downloading.

[[models]]
name = "Qwen3.5-2B-Instruct"
file = "Qwen3.5-2B-Instruct-Q4_K_M.gguf"
profile = "small"
approx_size_gb = 1.5
use = "smallest hosts: 4 cores, 4 GB free memory"
sha256 = ""
tested = ""

[[models]]
name = "Gemma-4-E2B-it"
file = "gemma-4-E2B-it-Q4_K_M.gguf"
profile = "small"
approx_size_gb = 1.6
use = "smallest hosts: 4 cores, 4 GB free memory"
sha256 = ""
tested = ""

[[models]]
name = "Qwen3.5-4B-Instruct"
file = "Qwen3.5-4B-Instruct-Q4_K_M.gguf"
profile = "small"
approx_size_gb = 2.7
use = "default: 4-8 cores, 8 GB free memory, no GPU"
sha256 = ""
tested = ""

[[models]]
name = "Gemma-4-E4B-it"
file = "gemma-4-E4B-it-Q4_K_M.gguf"
profile = "small"
approx_size_gb = 3.0
use = "default: 4-8 cores, 8 GB free memory, no GPU"
sha256 = ""
tested = ""

[[models]]
name = "Qwen3.5-9B-Instruct"
file = "Qwen3.5-9B-Instruct-Q4_K_M.gguf"
profile = "medium"
approx_size_gb = 5.8
use = "a GPU with 8 GB, or 16 GB of memory"
sha256 = ""
tested = ""

[[models]]
name = "Gemma-4-26B-A4B-it"
file = "gemma-4-26B-A4B-it-Q4_K_M.gguf"
profile = "large"
approx_size_gb = 16
use = "32 GB of memory without a GPU (mixture of experts, ~4B active), or a large GPU"
sha256 = ""
tested = ""
